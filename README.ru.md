# fly.board

![fly.board logo](img/logo.png)

> Один из немногих простых движков блогов, у которых потребление памяти почти не растёт с ростом числа соединений: **~104 МБ RSS** в простое (4 воркера; на реальном production-сервере с одним воркером поддерживает **68–120 МБ**), и всё ещё **~110–146 МБ** под нагрузкой C10k, C100k и даже C1m.
> Лёгкий гибрид форума и блога на основе C-фреймворка CWIST с поддержкой HTTPS/3, Argon2id, PQC-подписей и NATS.

## Возможности

- **Эффективное использование памяти и масштабируемость по соединениям** — реализация на C со стеком и кучей. **~104 МБ RSS** в простое; RSS остаётся около **~110–146 МБ** от C10k до C1m одновременных соединений.
- **Современный транспорт** — TLS 1.3 + HTTP/3 (QUIC) по умолчанию. Опциональный ECH (Encrypted Client Hello).
- **Безопасная аутентификация** — клиентский прехеш SHA-512 + серверный **Argon2id** (KDF из OpenSSL 3). JWT-куки для сессий.
- **Гибрид форум/блог** — Markdown-посты на основе slug + несколько досок + вложенные комментарии.
- **Предпросмотр в реальном времени** — мгновенный рендеринг предпросмотра на стороне сервера из Markdown-редактора.
- **PQC-подписи** — прикрепление и проверка постквантовых криптографических подписей к постам.
- **Файловое хранилище** — ≤1 МБ в SQLite, файлы больше — на томе. Автовстраивание изображений/видео/аудио.
- **Интеграция NATS** — распределённый шлюз сообщений через переменную окружения `NATS_URL`.
- **Тёмная тема** — переключение темы на основе Cookie с динамическими CSS-переменными.

## Сборка

```sh
make
./keygen.sh
```

Зависимости:
- [CWIST](https://github.com/religiya-serdtsa/cwist) — TLS 1.3 / HTTP/3 (QUIC) обрабатывается встроенным BoringSSL внутри CWIST; дополнительная установка не требуется.
- OpenSSL 3.x (Argon2id KDF)
- ngtcp2 / nghttp3 (HTTP/3)
- cJSON, SQLite3

`Makefile` клонирует и собирает `third_party/md4c` в виде статической библиотеки.

## Запуск

```sh
./fly_board
```

Порт по умолчанию берётся из значения `port` в файле `blog.settings` (по умолчанию 9443).

```text
https://localhost:9443
```

HTTP/3 слушает на том же порту по UDP.

### Включение ECH (опционально)

```sh
BLOG_ECH_KEY=ech/server.ech ./fly_board
# или
BLOG_ECH_DIR=ech ./fly_board
```

Если сборка OpenSSL не поддерживает ECH, в лог будет записано предупреждение, а сервер продолжит работу с обычным HTTPS/3.

### Интеграция NATS (опционально)

```sh
NATS_URL=nats://localhost:4222 ./fly_board
```

## Ключевые возможности

| Возможность | Путь | Описание |
|---------|------|-------------|
| Главная | `/` | Список последних постов |
| Доски | `/boards` | Управление несколькими досками (только для администраторов) |
| Пост | `/post/:slug` | рендеринг md4c Markdown + комментарии + вложения |
| Вход/Регистрация | `/login`, `/register` | Argon2id + JWT-куки |
| Профиль | `/profile` | Никнейм, биография, аватар, дата регистрации |
| Настройки аккаунта | `/account/settings` | Редактирование профиля |
| Смена пароля | `/account/password` | Проверка текущего пароля, повторный хеш Argon2id |
| Админка | `/admin/users` | Изменение ролей пользователей, удаление пользователей |
| Файловое хранилище | `/files` | Загрузка/скачивание/удаление |

## Конфигурация

- `blog.settings` — заголовок блога, подзаголовок, подвал, порт и лимиты загрузки
- `admin.settings` — учётная запись администратора (2 строки: `username`\n`password`)

## База данных

SQLite3 (`data/blog.db`). Схема автоматически мигрируется при запуске приложения.

```
users       — аккаунты, хеши Argon2id, роли, профили
boards      — название/slug/описание доски/admin_only
posts       — тело Markdown, PQC-подпись, краткое содержание
files       — путь/размер/MIME вложения
comments    — вложенные комментарии (target_type, parent_id)
board_permissions — права доступа к приватным доскам
```

## Архитектура

```
CWIST (HTTP/3, TLS 1.3)
  ├── src/auth/     — Argon2id, JWT, сессии
  ├── src/db/       — SQLite3 CRUD
  ├── src/handlers/ — маршрутизация/бизнес-логика
  ├── src/render/   — cwist_html_element SSR + md4c
  ├── src/crypto/   — PQC подпись/проверка
  └── src/nats/     — Pub/Sub сообщений
```

## Лицензия

MIT License

---

## Бенчмарк масштабируемости

### Что измеряет этот бенчмарк

Эти тесты используют `h2load` **с опцией `-r`** (ограничение скорости). Они намеренно **не** являются тестами максимальной пропускной способности. Вместо этого они измеряют, может ли сервер **удерживать огромное количество одновременных HTTP/2-соединений**, обрабатывая контролируемую скорость запросов на процесс.

Поскольку нагрузка ограничена по скорости:

- Отображаемый **RPS отражает настроенную скорость запросов**, а не абсолютный потолок пропускной способности сервера.
- Главный метрика — **стабильность resident-set-size (RSS)** по мере роста числа соединений от 10 000 до 1 000 000.

Число воркеров масштабируется вместе с нагрузкой, чтобы каждый тест оставался реалистичным: **4 воркера** для C10k, **12 воркеров** для C100k и **12 воркеров** для C1m. Это также объясняет различия в показателях использования CPU между тремя запусками.

### Окружение хоста

| Параметр | Значение |
|------|-------|
| ОС | Linux 7.1.0-mountain-rc6+ |
| Архитектура | x86_64 |
| CPU | 12 logical cores |
| RAM | 62 GiB |
| GCC | 14.2.0 (Debian 14.2.0-19) |
| OpenSSL | 3.5.6 |
| Инструмент | h2load nghttp2/1.64.0 |
| CWIST | `libcwist.a` из соседнего чекаута cwist (2026-08-29, arena bump-аллокатор, общая req/res-арена, стеки воркеров 256 КБ, упрочнение HTTP/3, sharded TLS handshake shepherd) |

### Настройка системы

| Параметр | Значение |
|-----------|-------|
| ulimit -n | 1,050,000 |
| fs.file-max | 2,097,152 |
| fs.nr_open | 1,050,000 |
| net.core.somaxconn | 1,050,000 |
| net.ipv4.tcp_max_syn_backlog | 1,050,000 |
| net.ipv4.ip_local_port_range | 1024 65535 |
| vm.max_map_count | 1,048,576 |
| kernel.pid_max | 4,194,304 |
| CPU governor | ecodemand |

### Использование памяти

| Состояние | RSS | Δ от предыдущего | Примечания |
|-------|-----|-----------------|-------|
| Простой | **~104 МБ** (106,192 КБ) | — | 4 workers, no connections |
| C10k | **~110 МБ** (112,436 КБ) | +6,244 КБ | 10,000 concurrent connections |
| C100k | **~146 МБ** (148,848 КБ) | +36,412 КБ | 100,000 concurrent connections |
| C1m churn | **~146 МБ** (149,816 КБ) | +968 КБ | 100k held TLS conns, 1M-request churn |

Общее изменение RSS **от C100k до прогона C1m churn составляет +968 КБ** — по сути, это шум измерения. Это самый важный результат бенчмарка.

Значения RSS — это **Maximum resident set size (kbytes)**, сообщаемые `/usr/bin/time -v` для процесса сервера.

### Стоимость памяти

| Переход | Δ RSS | Δ соединений | Приблизительная стоимость на одно дополнительное соединение |
|---|---|---|---|
| Idle → C10k | +6,244 КБ | 10 000 | ~0.6 КБ / соединение |
| C10k → C1m churn | +37,380 КБ | — | ~0.4 КБ / доп. удерживаемое соединение; C100k → C1m: +968 КБ (шум) |

Начальный скачок от Idle до C10k — это авансовая оплата за состояние TLS, буферы соединений и накладные расходы воркеров. От C10k до C100k стоимость держится около ~0.4 КБ на дополнительное удерживаемое соединение, а изменение RSS от C100k до C1m (+968 КБ) укладывается в шум измерения — стоимость памяти на соединение практически не растёт.

### Тест C10k с одновременными соединениями

Измерено с `h2load`, поддерживающим 10,000 concurrent connections.

| Параметр | Значение |
|------|-------|
| Воркеры | 4 |
| Одновременные соединения | 10,000 |
| Длительность | 12.05 s |
| Максимальный RSS | **~110 МБ** (112,436 КБ) |
| Использование CPU | ~365% |
| User time | 41.05 s |
| System time | 3.04 s |
| Major page faults | 2 |
| Minor page faults | 16,948 |
| Voluntary context switches | 58,050 |
| Involuntary context switches | 14,828 |
| File system outputs | 256 |
| Всего запросов | 20000 |
| Всего успешно | 20000 |
| Всего неудач | 0 |
| Прибл. общий RPS | **2285.22** |
| Уровень успеха | **100.00%** |
| Код завершения | **0** |

### Тест C100k с одновременными соединениями

Измерено с `h2load`, поддерживающим 100,000 concurrent connections.

| Параметр | Значение |
|------|-------|
| Воркеры | 12 |
| Одновременные соединения | 100,000 |
| Длительность | 1:23.49 |
| Максимальный RSS | **~146 МБ** (148,848 КБ) |
| Использование CPU | ~815% |
| User time | 653.83 s |
| System time | 26.78 s |
| Major page faults | 0 |
| Minor page faults | 76,332 |
| Voluntary context switches | 446,557 |
| Involuntary context switches | 617,777 |
| File system outputs | 336 |
| Всего запросов | 200000 |
| Всего успешно | 200000 |
| Всего неудач | 0 |
| Прибл. общий RPS | **2785.16** |
| Уровень успеха | **100.00%** |
| Код завершения | **0** |

### Тест C1m churn (переработан 2026-08-23, исправлен 2026-08-24)

Прежняя цель «1,000,000 одновременных TLS-соединений» снята: HTTPS-путь занимает один поток воркера на каждое живое соединение, поэтому удерживаемая конкурентность ограничена произведением воркеров на потоки — намного ниже 1M. (**Открытый** HTTP/1.x-путь cwist событийно-ориентирован и достиг 1,000,000/1,000,000 удерживаемых соединений — см. README cwist.) Тест C1m измеряет churn: 20 процессов h2load x 50,000 запросов поверх 100,000 одновременно удерживаемых TLS-соединений, с ограничением по watchdog.

| Параметр | Значение |
|------|-------|
| Воркеры | 12 |
| Форма нагрузки | 20 x (-c 5000 -n 50000 -r 1000 -T 30) |
| Итого | 1,000,000 запросов поверх 100,000 удерживаемых соединений |
| Результат | **завершён — без ступора** |
| Всего успешно | **1,000,000 / 1,000,000 (100.0%)** |
| Ошибок | 0 |
| Время выполнения | ~1:36 (h2load «finished in» 63.7-89.3 s на процесс) |
| Фантомные соединения | 0 (счётчики ESTABLISHED клиента и сервера совпали) |
| Завершение сервера | чистое, exit 0 |

История: прогон той же нагрузки 2026-08-23 завис на ~85k соединений. Корневая причина (исправлена в cwist `perf(https): non-blocking TLS handshake shepherd`): TLS-handshake выполнялся синхронно внутри потоков воркеров с 30-секундными ожиданиями poll, поэтому несколько сотен медленных клиентов занимали весь пул, очередь accept переполнялась, а избыточные handshake молча отбрасывались, оставляя клиентов в ESTABLISHED без серверного сокета. Теперь handshake выполняется в неблокирующем потоке-shepherd; воркеры пула занимают только установленные сессии.

> Примечание: значения измерены при поддержании реальных клиентских соединений по HTTP/2 (TLS 1.3). Число воркеров отличается в каждом тесте; см. раздел «Что измеряет этот бенчмарк».

**Ключевые выводы**

- **Масштабируемость по соединениям**: RSS остаётся около **~110–146 МБ** от 10 000 до 1 000 000 одновременных соединений. Память на одно соединение практически не растёт.
- **Стабильность при реалистичной нагрузке**: C10k завершился со **100% успехом**, C100k — с **100.00%**, оставаясь в том же диапазоне памяти.
- **Память держится и на масштабе C1m**: прогон C1m churn (1M запросов поверх 100k удерживаемых TLS-соединений) завершается без ступора со **100% успехом**, RSS остаётся ~146 МБ — ни памяти-спирали, ни падения.
- **Безопасность данных**: SQLite безопасно сохранила все данные при SIGINT (256 FS outputs at C10k).

### Бенчмарк пропускной способности

Бенчмарк выше измеряет **connection scalability**, а не абсолютную **request throughput**. Чтобы измерить сырой потолок пропускной способности сервера, был запущен неограниченный тест с помощью `h2load` (без ограничения скорости `-r`) поверх HTTP/2.

| Item | Value |
|------|-------|
| Command | `h2load -c512 -n100000 https://127.0.0.1:8888/` |
| Workers | 12 |
| Concurrent connections | 512 |
| Total requests | 100,000 |
| Succeeded | 100,000 |
| Failed / Errored / Timeout | 0 |
| Duration | 13.95 s |
| Mean RPS | **7167.28** |
| Mean throughput | **290.51 MB/s** |
| Задержка запроса (h2load `time for request`) | min 183 µs, mean 30.69 ms, max 209.00 ms, sd 11.18 ms |
| Приблизительная перцентильная задержка* | p50 ~30.7 ms, p95 ~49.1 ms, p99 ~56.7 ms |

\* Перцентили оценены по сообщённым среднему и стандартному отклонению; h2load по умолчанию выводит только min/max/mean/sd. Для точной гистограммы перцентилей запустите с опцией `--latency-collect`.

#### Сравнение с HTTP/1.1 через `wrk`

Для сравнения, тот же endpoint был протестирован с помощью `wrk` поверх HTTP/1.1. Поскольку протокол и инструмент разные, приведённые ниже цифры **нельзя напрямую сравнивать** с результатами HTTP/2 h2load выше.

| Item | Value |
|------|-------|
| Command | `wrk -t12 -c512 -d60s https://127.0.0.1:8888/` |
| Duration | 60 s |
| Requests/sec | **1282.49** |
| Transfer/sec | 52.29 MB |
| Latency | Avg 138.61 ms, Stdev 39.26 ms, Max 311.70 ms |

Эти цифры показывают абсолютный потолок пропускной способности движка под сфокусированной неограниченной нагрузкой. Они отделены от тестов масштабируемости по соединениям выше.
