Inline math $x^2 + y^2 = z^2$ in a sentence.

$$
\int_{-\infty}^{\infty} e^{-x^2} \, dx = \sqrt{\pi}
$$

Escaped dollar \$100 plus $a$ math.

A bare expression \( \alpha + \beta \) inline and a display:

\[ E = mc^2 \]

Code must not parse math: `$not_math$` and `$$also_not$$`.

```python
def f(x):
    return x $ 1  # dollar in code
```
