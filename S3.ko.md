# S3 오브젝트 스토리지 (선택)

fly.board는 기본적으로 업로드된 파일을 `public/uploads/` 아래 로컬 디스크에 저장합니다.
`s3.settings`를 채워 넣으면 업로드를 S3 호환 오브젝트 스토리지(AWS S3, MinIO,
Cloudflare R2, Backblaze B2 등)에 **함께**(또는 **대신**) 저장할 수 있습니다.

`s3.settings`는 **선택 사항**입니다. 파일이 없으면 첫 실행 시 빈 템플릿이 생성되고
서버는 계속 로컬 저장소만 사용합니다. **모든 값이 비어 있거나** `endpoint`,
`bucket`, `access_key`, `secret_key` 중 하나라도 비어 있으면 S3는 꺼진 것으로
간주하며 아무것도 달라지지 않습니다.

## 동작 방식

두 업로드 경로(단순 multipart 업로드와 TASFA 청크 업로드) 모두 마지막에 파일을
`public/uploads/`에 쓰고 썸네일/프리뷰를 로컬에서 생성합니다 — 이 부분은 절대
변하지 않습니다. S3가 켜져 있으면 그 다음에:

1. 완성된 파일이 Signature V4 인증으로 `<prefix><basename>` 키로 버킷에 PUT됩니다.
2. 이후 동작은 `mode`에 따라 달라집니다:
   - `mirror` (기본): 로컬 사본을 유지합니다. S3는 실시간 백업/외부 사본이며,
     다운로드는 이전과 똑같이 로컬 디스크에서 서빙됩니다.
   - `offload`: 로컬 사본을 삭제하고 DB 행을 `s3://<key>`로 표시합니다.
     다운로드는 1시간 유효한 presigned GET URL로 302 리다이렉트됩니다.
3. PUT이 실패하면 업로드는 로컬 디스크에 남고 에러만 로그됩니다 — 버킷 설정
   오류나 장애가 업로드를 깨뜨리지 않습니다.

썸네일과 프리뷰(`.thumbs/`, `.previews/`)는 항상 로컬에 남고, 원본 파일만
미러/오프로드됩니다.

삭제(`/file/delete`, 글 삭제, 중복 정리, `drop_all`)는 로컬 파일과 S3 객체 중
존재하는 쪽을 모두 지웁니다. S3 DELETE는 멱등이므로 모드 전환 후 정리도 안전합니다.

## 설정 참조 (`s3.settings`)

`blog.settings`와 같은 `key=value` 형식. 비어 있으면 미설정입니다.

| 키 | 필수 | 설명 |
|-----|------|------|
| `endpoint` | 예 | S3 엔드포인트 기본 URL. 예: `https://s3.us-east-1.amazonaws.com`, `https://minio.example.com:9000` |
| `region` | 권장 | 서명 리전. 예: `us-east-1`. 비어 있으면 `us-east-1`로 간주 (MinIO는 아무 값이나 허용) |
| `bucket` | 예 | 대상 버킷 이름. 버킷은 미리 만들어져 있어야 함 |
| `access_key` | 예 | Access key id |
| `secret_key` | 예 | Secret access key |
| `prefix` | 아니오 | 모든 객체 앞에 붙는 키 프리픽스. 예: `uploads/` |
| `use_path_style` | 아니오 | `true` = `https://endpoint/bucket/key` (MinIO 등 자체 호스팅 게이트웨이에 필요). 비어 있음/`false` = virtual-host 방식 `https://bucket.endpoint/key` (AWS 기본) |
| `mode` | 아니오 | `mirror`(기본)는 로컬 파일을 유지하며 S3에도 저장, `offload`는 S3로 옮기고 로컬 사본 삭제 |

`#` 주석 줄을 쓸 수 있습니다. 자격 증명이 들어가므로 `s3.settings`는
`.gitignore`에 등록되어 있습니다.

## 설정 예시

### MinIO (자체 호스팅, mirror 모드)

```ini
endpoint=https://minio.example.com:9000
region=us-east-1
bucket=flyboard-uploads
access_key=flyboard
secret_key=change-me-long-random
prefix=uploads/
use_path_style=true
mode=mirror
```

### AWS S3 (offload 모드)

```ini
endpoint=https://s3.ap-northeast-2.amazonaws.com
region=ap-northeast-2
bucket=my-blog-uploads
access_key=AKIA...
secret_key=...
prefix=flyboard/
use_path_style=false
mode=offload
```

### Cloudflare R2 (offload 모드)

```ini
endpoint=https://<account-id>.r2.cloudflarestorage.com
region=auto
bucket=my-blog-uploads
access_key=...
secret_key=...
prefix=uploads/
use_path_style=true
mode=offload
```

## 운영 참고

- **IAM 권한**: 자격 증명에 버킷 대상 `s3:PutObject`, `s3:DeleteObject`가 필요하고,
  `offload`를 쓰면 `s3:GetObject`도 필요합니다(presigned URL이 이 자격 증명으로 서명됨).
- **TLS**: `https://` 엔드포인트를 사용하세요. 페이로드 서명은 TLS에서만 안전한
  `UNSIGNED-PAYLOAD`를 사용합니다.
- **지연 시간**: S3 PUT은 업로드 요청 안에서 동기적으로 일어나므로 느린 버킷은
  업로드를 느리게 만듭니다(mirror, offload 공통).
- **Presigned 다운로드** (`offload`): 클라이언트가 버킷으로 직접 리다이렉트되므로
  브라우저에서 버킷 엔드포인트에 도달할 수 있어야 합니다. URL은 1시간 후 만료됩니다.
- **모드 전환**: `mirror` ↔ `offload` 전환은 기존 파일을 마이그레이션하지 않습니다.
  이전에 오프로드된 행(`s3://` 마커)은 계속 presigned 리다이렉트로, 로컬 행은 디스크에서
  서빙됩니다. 삭제는 양쪽 모두 정리합니다.
